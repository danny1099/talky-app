import { Inter, Raleway, Comfortaa } from 'next/font/google'

export const globalFont = Inter({ subsets: ['latin'] })
export const titleFont = Raleway({ subsets: ['latin'] })
export const brandFont = Comfortaa({ subsets: ['latin'] })
